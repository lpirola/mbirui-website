<style is:inline>
  @import url('https://fonts.googleapis.com/css2?family=Fraunces:opsz,wght@9..144,500;9..144,600;9..144,700&family=IBM+Plex+Mono:wght@400;500;600&family=Instrument+Sans:wght@400;500;600;700&display=swap');

  :root {
    --aw-font-sans: 'Instrument Sans';
    --aw-font-serif: 'Fraunces';
    --aw-font-heading: 'Fraunces';
    --aw-font-mono: 'IBM Plex Mono';

    --aw-color-primary: rgb(32 74 67);
    --aw-color-secondary: rgb(140 74 47);
    --aw-color-accent: rgb(18 60 105);

    --aw-color-text-heading: rgb(42 38 34);
    --aw-color-text-default: rgb(65 58 51);
    --aw-color-text-muted: rgb(101 93 85 / 86%);
    --aw-color-text-page: rgb(42 38 34);

    --aw-color-bg-page: rgb(251 250 247);
    --aw-color-bg-page-dark: rgb(23 22 20);
    --aw-color-surface: rgb(245 241 232 / 88%);
    --aw-color-surface-strong: rgb(222 211 190);
    --aw-color-line: rgb(42 38 34 / 14%);
    --aw-color-glow: rgb(32 74 67 / 10%);

    --aw-color-success: rgb(45 106 79);
    --aw-color-warning: rgb(163 101 26);
    --aw-color-error: rgb(156 47 47);
    --aw-color-info: rgb(18 60 105);

    --aw-radius-sm: 0.5rem;
    --aw-radius-md: 0.875rem;
    --aw-radius-lg: 1.5rem;
    --aw-radius-xl: 2.25rem;
    --aw-shadow-soft: 0 1.25rem 4rem rgb(26 22 18 / 0.1);
    --aw-shadow-strong: 0 1.75rem 5rem rgb(26 22 18 / 0.14);

    --aw-pattern-ring:
      radial-gradient(circle at center, transparent 58%, rgb(18 60 105 / 0.2) 58.5%, transparent 60.5%),
      radial-gradient(circle at center, transparent 70%, rgb(140 74 47 / 0.22) 70.5%, transparent 72.5%),
      repeating-conic-gradient(
        from 8deg,
        rgb(32 74 67 / 0.92) 0deg 8deg,
        rgb(251 250 247 / 0.98) 8deg 14deg,
        rgb(140 74 47 / 0.78) 14deg 20deg,
        rgb(251 250 247 / 0.98) 20deg 26deg
      );
  }

  .dark {
    --aw-color-primary: rgb(126 177 167);
    --aw-color-secondary: rgb(207 137 99);
    --aw-color-accent: rgb(127 168 214);

    --aw-color-text-heading: rgb(245 241 232);
    --aw-color-text-default: rgb(224 217 207);
    --aw-color-text-muted: rgb(193 183 168 / 84%);
    --aw-color-text-page: rgb(245 241 232);

    --aw-color-bg-page: rgb(23 22 20);
    --aw-color-bg-page-dark: rgb(23 22 20);
    --aw-color-surface: rgb(33 31 28 / 88%);
    --aw-color-surface-strong: rgb(42 39 35);
    --aw-color-line: rgb(255 248 238 / 10%);
    --aw-color-glow: rgb(127 168 214 / 10%);

    --aw-color-success: rgb(122 195 155);
    --aw-color-warning: rgb(215 157 83);
    --aw-color-error: rgb(226 144 144);
    --aw-color-info: rgb(127 168 214);

    --aw-pattern-ring:
      radial-gradient(circle at center, transparent 58%, rgb(127 168 214 / 0.26) 58.5%, transparent 60.5%),
      radial-gradient(circle at center, transparent 70%, rgb(207 137 99 / 0.24) 70.5%, transparent 72.5%),
      repeating-conic-gradient(
        from 8deg,
        rgb(126 177 167 / 0.82) 0deg 8deg,
        rgb(23 22 20 / 0.98) 8deg 14deg,
        rgb(207 137 99 / 0.78) 14deg 20deg,
        rgb(23 22 20 / 0.98) 20deg 26deg
      );
  }

  ::selection {
    background-color: rgb(18 60 105 / 22%);
    color: var(--aw-color-text-heading);
  }

  html {
    background:
      radial-gradient(circle at top left, rgb(140 74 47 / 0.12), transparent 28%),
      radial-gradient(circle at 88% 12%, rgb(18 60 105 / 0.14), transparent 24%),
      linear-gradient(180deg, var(--aw-color-bg-page), rgb(255 255 255 / 0.05));
    color: var(--aw-color-text-page);
  }

  body {
    color: var(--aw-color-text-default);
    background:
      linear-gradient(180deg, rgb(255 255 255 / 0.18), transparent 20rem),
      radial-gradient(circle at 22% 10%, rgb(140 74 47 / 0.08), transparent 22rem), var(--aw-color-bg-page);
    font-family: var(--aw-font-sans), sans-serif;
  }

  body::before {
    content: '';
    position: fixed;
    inset: 0;
    pointer-events: none;
    opacity: 0.45;
    background-image:
      linear-gradient(rgb(42 38 34 / 0.03) 1px, transparent 1px),
      linear-gradient(90deg, rgb(42 38 34 / 0.03) 1px, transparent 1px);
    background-size: 2.625rem 2.625rem;
    mask-image: radial-gradient(circle at center, rgb(0 0 0 / 0.45), transparent 82%);
  }

  body::after {
    content: '';
    position: fixed;
    inset: 0;
    pointer-events: none;
    opacity: 0.06;
    background-image: radial-gradient(rgb(42 38 34 / 0.92) 0.8px, transparent 0.8px);
    background-size: 18px 18px;
    mix-blend-mode: multiply;
  }

  h1,
  h2,
  h3,
  h4,
  h5,
  h6 {
    color: var(--aw-color-text-heading);
    font-family: var(--aw-font-heading), serif;
    font-feature-settings: 'ss01' 1;
    font-variation-settings:
      'SOFT' 50,
      'WONK' 0;
    letter-spacing: -0.035em;
    text-wrap: balance;
  }

  strong,
  b {
    color: var(--aw-color-text-heading);
  }

  code,
  pre,
  .font-mono {
    font-family: var(--aw-font-mono), monospace;
  }

  .section-kicker {
    color: var(--aw-color-accent);
    font-family: var(--aw-font-mono), monospace;
    letter-spacing: 0.16em;
    text-transform: uppercase;
    font-size: 0.72rem;
    font-weight: 600;
  }

  .editorial-frame {
    position: relative;
    overflow: clip;
    border: 1px solid var(--aw-color-line);
    background:
      linear-gradient(180deg, rgb(255 255 255 / 0.3), transparent 24%),
      linear-gradient(180deg, rgb(255 255 255 / 0.55), var(--aw-color-surface));
    box-shadow: var(--aw-shadow-soft);
    backdrop-filter: blur(10px);
  }

  .editorial-frame::before {
    content: '';
    position: absolute;
    inset: 1rem;
    border: 1px solid rgb(42 38 34 / 0.08);
    border-radius: inherit;
    pointer-events: none;
  }

  .editorial-ring {
    position: relative;
    isolation: isolate;
  }

  .editorial-ring::before {
    content: '';
    position: absolute;
    inset: 0;
    border-radius: 999px;
    background: var(--aw-pattern-ring);
    opacity: 0.95;
    pointer-events: none;
  }

  #header > div:first-child {
    border-bottom: 1px solid transparent;
  }

  #header.scroll > div:first-child {
    border-bottom-color: var(--aw-color-line);
    background: color-mix(in srgb, var(--aw-color-bg-page) 86%, white 14%);
    box-shadow: 0 0.9rem 2.5rem rgb(26 22 18 / 0.08);
  }

  footer {
    background:
      linear-gradient(180deg, rgb(255 255 255 / 0.1), transparent 18%),
      linear-gradient(180deg, rgb(245 241 232 / 0.84), rgb(222 211 190 / 0.56));
  }

  .dark footer {
    background: linear-gradient(180deg, rgb(42 39 35 / 0.9), rgb(23 22 20));
  }
</style>
